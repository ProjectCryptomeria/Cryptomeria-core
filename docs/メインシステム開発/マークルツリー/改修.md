# 🛠️ Project Cryptomeria Core: Trustless Gateway Protocol & Implementation Report

## 1. 概要と目的 (Overview & Objective)

本アップデートの主目的は、**Webコンテンツの改ざん検知機能** と **非カストディ型（ユーザー主権型）アップロードフロー** の確立です。

従来のGWC（Gateway Chain）は、ユーザーからZipファイルを受け取り、即座にブロックチェーンへ書き込む「信頼ベース」のルーターとして機能していました。しかし、このモデルでは「GWCが悪意を持ってファイルを差し替えた場合」や「通信経路での改ざん」をクライアントが検証する術がありませんでした。

今回の改修により、**ゼロトラスト思想**に基づき、クライアントが展開後のコンテンツ内容（SiteRoot）に電子署名を行うプロセスを導入しました。これにより、ブロックチェーンに保存されるデータが「ユーザーが意図した通りのものであること」を数学的に保証します。

---

## 2. トラストレス・ゲートウェイ・プロトコル仕様 (Protocol Specification)

本システムの中核となる、GWCを信用せずにデータの完全性を保証するためのアルゴリズムと通信フローの詳細です。

### 2.1 コアアルゴリズム：決定論的マークルツリー (Deterministic Merkle Tree)

アップロードされたコンテンツ（Zip）から、一意かつ再現可能な「指紋（SiteRoot）」を生成します。

#### A. 正規化 (Canonicalization)
Zipファイル自体のハッシュ値は作成ツールによる差異が出るため使用しません。展開後の「論理的なコンテンツ」を正規化してハッシュ計算の対象とします。

1.  **展開 (Unzip)**: メモリ上でZipを展開。
2.  **フィルタリング**: ディレクトリ、隠しファイル、シンボリックリンクを除外。
3.  **ソート (Sorting)**: ファイルパス（例: `index.html`, `js/main.js`）で辞書順（A-Z）にソート。
    * これにより、Zip内の格納順序に関わらず、常に同じ順序でハッシュ計算が行われます。

#### B. ハッシュツリー構造
正規化されたファイルリストから、ボトムアップでハッシュを積み上げます。

* **Level 1: フラグメント葉 (Fragment Leaf)**
    各ファイルを固定サイズで分割し、位置情報を含めてハッシュ化します（並べ替え攻撃対策）。
    $$
    Leaf_{frag} = \text{SHA256}(\text{"FRAG"} + \text{FilePath} + \text{Index} + \text{Hash(Data)})
    $$

* **Level 2: ファイルルート (File Root)**
    あるファイルの全ての $Leaf_{frag}$ をマークルツリーとして集約します。
    $$
    Root_{file} = \text{MerkleRoot}([Leaf_{frag\_0}, Leaf_{frag\_1}, ...])
    $$

* **Level 3: ファイルエントリ (File Entry)**
    ファイルパスとサイズを含めることで、メタデータの改ざんを防ぎます。
    $$
    Leaf_{file} = \text{SHA256}(\text{"FILE"} + \text{FilePath} + \text{Size} + Root_{file})
    $$

* **Level 4: サイトルート (Site Root)**
    全ファイルの $Leaf_{file}$ を集約した、最終的なルートハッシュです。
    $$
    \text{SiteRoot} = \text{MerkleRoot}([Leaf_{file\_0}, Leaf_{file\_1}, ...])
    $$

---

### 2.2 通信プロトコル：4ウェイ・ハンドシェイク

クライアントとGWCの間で、ステートフルなセッションを通じて合意形成を行います。

#### Step 1: Initialization (セッション開始)
* **Client**: 「プロジェクト名、予想サイズ」を通知 (`MsgInitUpload`)。
* **GWC**: 一意な `UploadID` を発行し、KVStoreにセッション領域を確保。イベント `init_upload` を発行。

#### Step 2: Data Transfer (データ転送)
* **Client**: Zipデータを分割して送信 (`MsgPostChunk`)。
* **GWC**: 受け取ったデータを一時領域のバッファに追記保存。この時点では中身の検証は行わない。

#### Step 3: Computation & Commitment (計算と提示)
* **Client**: 送信完了を通知し、計算を要求 (`MsgCompleteUpload`)。
* **GWC**:
    1.  バッファ内のZipを展開・正規化。
    2.  上記のアルゴリズムで **`SiteRoot`** を計算。
    3.  イベント `complete_upload` を発行し、クライアントに `SiteRoot` を提示。
* **Client (Verify)**: 手元のZipからローカルで同様に `SiteRoot` を計算し、GWCから提示された値と一致するか確認。

#### Step 4: Signing & Distribution (署名と配信)
* **Client**: 一致を確認後、その `SiteRoot` に対して秘密鍵で署名を作成し送信 (`MsgSignUpload`)。
* **GWC**:
    1.  署名対象の `SiteRoot` が、保持している計算結果と一致するか確認。
    2.  一致すれば「承認」とみなし、MDSC（Manifest）およびFDSC（Fragment）へ **IBCパケット** として並列配信。
    3.  パケット内に `SiteRoot`, `ClientSignature` を封入。

---

## 3. コンポーネント別 実装詳細 (Detailed Implementation)

### 🛡️ GWC (Gateway Chain)
検証可能なルーターとしての機能を実装しました。

* **Merkle Logic (`x/gateway/keeper/merkle_logic.go`)**:
    * 正規化（ソート）と `LeafFrag` -> `FileRoot` -> `SiteRoot` の計算ロジックを実装。
* **イベント発行 (`x/gateway/keeper/msg_server.go`)**:
    * 外部クライアントが自動化できるよう、`ctx.EventManager().EmitEvent(...)` を追加。
* **パケット拡張 (`proto/gwc/gateway/v1/packet.proto`)**:
    * `ManifestPacket` と `FragmentPacket` に検証用フィールドを追加。

### 📖 MDSC (Manifest Data Store Chain)
検証情報を永続化するように拡張しました。

* **Proto定義 (`proto/mdsc/metastore/v1/manifest.proto`)**:
    * `Manifest` メッセージに `site_root`, `client_signature`, `fragment_size` を追加。
* **IBC受信ロジック (`x/metastore/module/module_ibc.go`)**:
    * `OnRecvPacket` でパケット内の拡張フィールドを読み取り、Manifest構造体へマッピングして保存。

### 💾 FDSC (Fragment Data Store Chain)
各断片のトレーサビリティを確保しました。

* **Proto定義 (`proto/fdsc/datastore/v1/fragment.proto`)**:
    * `Fragment` メッセージに `site_root` を追加。
* **IBC受信ロジック (`x/datastore/module/module_ibc.go`)**:
    * パケットから `site_root` を抽出し、バイナリデータと共にタグ付けして保存。

---

## 4. データモデルの変更点 (Data Model Schema Changes)

### Manifest (MDSC)
```protobuf
message Manifest {
  string project_name = 1;
  string version = 2;
  string creator = 3;
  map<string, FileInfo> files = 4;
  
  // ✨ Added Verification Fields
  string site_root = 5;       // 検証用ルートハッシュ
  bytes client_signature = 6; // 否認防止用署名
  uint64 fragment_size = 7;   // 検証時の再計算用パラメータ
}
```

### Fragment (FDSC)
```protobuf
message Fragment {
  string fragment_id = 1;
  bytes data = 2;
  string creator = 3;
  
  // ✨ Added Field
  string site_root = 4; // 所属するSiteRootへの参照
}
```

---

## 5. テストと検証 (Testing & Verification)

### ✅ E2Eテストスクリプト (`ops/scripts/test/poc/verify_signed_upload.sh`)
Kubernetes環境に対して、以下のフローを自動検証するスクリプトを整備しました。

1.  **依存関係チェック**: `jq` コマンドや鍵の存在確認。
2.  **動的ID抽出**: `InitUpload` のTxイベントから `jq` を用いて `upload_id` を自動抽出。
3.  **ルートハッシュ抽出**: `CompleteUpload` のTxイベントから `site_root` を自動抽出。
4.  **署名送信**: ローカルで確認したRootに対しダミー署名を用いて `SignUpload` を実行。

### ✅ 検証コマンド (`just test verify`)
アップロード完了後、保存されたデータを検証します。

* **Manifest確認**: MDSCから `Manifest` を取得し、`site_root` や `client_signature` が正しく保存されていることを確認。
* **Fragment確認**: FDSCから `Fragment` を取得し、`site_root` が付与されていることを確認。

---

## 6. 今後の課題 (Future Work)

1.  **署名検証の厳密化**:
    * 現在は署名の存在確認のみ。`MsgSignUpload` 内で `secp256k1` 等を用いて「署名者が `Creator` であるか」「署名対象が `SiteRoot` と一致するか」を検証するロジックの実装が必要。
2.  **クライアントSDKの整備**:
    * ブラウザ上でZipを展開し、Go側と同じロジックで `SiteRoot` を計算・署名するJS/TSライブラリの開発。
3.  **エラーハンドリング**:
    * 不正な署名や不整合なデータが送られた際の拒否ロジックの強化。

---
*Implementation Date: 2026-01-24*
*Author: Project Cryptomeria Dev Team*
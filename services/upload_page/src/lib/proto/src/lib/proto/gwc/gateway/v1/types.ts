// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: gwc/gateway/v1/types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "gwc.gateway.v1";

export enum SessionState {
  SESSION_STATE_UNSPECIFIED = 0,
  SESSION_STATE_INIT = 1,
  SESSION_STATE_ROOT_COMMITTED = 2,
  SESSION_STATE_DISTRIBUTING = 3,
  /** SESSION_STATE_FINALIZING - manifest sent, waiting for MDSC ack */
  SESSION_STATE_FINALIZING = 4,
  SESSION_STATE_CLOSED_SUCCESS = 5,
  SESSION_STATE_CLOSED_FAILED = 6,
  UNRECOGNIZED = -1,
}

export function sessionStateFromJSON(object: any): SessionState {
  switch (object) {
    case 0:
    case "SESSION_STATE_UNSPECIFIED":
      return SessionState.SESSION_STATE_UNSPECIFIED;
    case 1:
    case "SESSION_STATE_INIT":
      return SessionState.SESSION_STATE_INIT;
    case 2:
    case "SESSION_STATE_ROOT_COMMITTED":
      return SessionState.SESSION_STATE_ROOT_COMMITTED;
    case 3:
    case "SESSION_STATE_DISTRIBUTING":
      return SessionState.SESSION_STATE_DISTRIBUTING;
    case 4:
    case "SESSION_STATE_FINALIZING":
      return SessionState.SESSION_STATE_FINALIZING;
    case 5:
    case "SESSION_STATE_CLOSED_SUCCESS":
      return SessionState.SESSION_STATE_CLOSED_SUCCESS;
    case 6:
    case "SESSION_STATE_CLOSED_FAILED":
      return SessionState.SESSION_STATE_CLOSED_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SessionState.UNRECOGNIZED;
  }
}

export function sessionStateToJSON(object: SessionState): string {
  switch (object) {
    case SessionState.SESSION_STATE_UNSPECIFIED:
      return "SESSION_STATE_UNSPECIFIED";
    case SessionState.SESSION_STATE_INIT:
      return "SESSION_STATE_INIT";
    case SessionState.SESSION_STATE_ROOT_COMMITTED:
      return "SESSION_STATE_ROOT_COMMITTED";
    case SessionState.SESSION_STATE_DISTRIBUTING:
      return "SESSION_STATE_DISTRIBUTING";
    case SessionState.SESSION_STATE_FINALIZING:
      return "SESSION_STATE_FINALIZING";
    case SessionState.SESSION_STATE_CLOSED_SUCCESS:
      return "SESSION_STATE_CLOSED_SUCCESS";
    case SessionState.SESSION_STATE_CLOSED_FAILED:
      return "SESSION_STATE_CLOSED_FAILED";
    case SessionState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** StorageInfo holds the connection details for a storage chain (MDSC/FDSC). */
export interface StorageInfo {
  /** channel_id is the local IBC channel ID connected to this chain (e.g., "channel-0") */
  channelId: string;
  /** chain_id is the chain ID of the connected storage chain (e.g., "fdsc-1") */
  chainId: string;
  /** api_endpoint is the HTTP API endpoint for downloading files (e.g., "http://localhost:1317") */
  apiEndpoint: string;
  /** connection_type indicates whether this is "mdsc" or "fdsc" */
  connectionType: string;
}

export interface MerkleStep {
  /** sibling_hex is a 32-byte hash encoded as hex string. */
  siblingHex: string;
  /** sibling_is_left indicates whether sibling is placed on the left side in concatenation. */
  siblingIsLeft: boolean;
}

export interface MerkleProof {
  steps: MerkleStep[];
}

export interface Session {
  sessionId: string;
  owner: string;
  executor: string;
  /** RootProof hex string. */
  rootProofHex: string;
  fragmentSize: Long;
  deadlineUnix: Long;
  state: SessionState;
  closeReason: string;
  /** counters for observability */
  distributedCount: Long;
  ackSuccessCount: Long;
  ackErrorCount: Long;
}

/** DistributeItem carries fragment bytes and its proofs for on-chain verification. */
export interface DistributeItem {
  path: string;
  index: Long;
  fragmentBytes: Uint8Array;
  /** fragment_proof proves fragment leaf -> file_root */
  fragmentProof?:
    | MerkleProof
    | undefined;
  /** file_size is the full size of the file in bytes */
  fileSize: Long;
  /** file_proof proves file leaf -> root_proof */
  fileProof?:
    | MerkleProof
    | undefined;
  /**
   * Optional: specify the target FDSC IBC channel to send this fragment to.
   * If empty, GWC will choose a channel (round-robin) from registered datastore channels.
   */
  targetFdscChannel: string;
}

function createBaseStorageInfo(): StorageInfo {
  return { channelId: "", chainId: "", apiEndpoint: "", connectionType: "" };
}

export const StorageInfo: MessageFns<StorageInfo> = {
  encode(message: StorageInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channelId !== "") {
      writer.uint32(10).string(message.channelId);
    }
    if (message.chainId !== "") {
      writer.uint32(18).string(message.chainId);
    }
    if (message.apiEndpoint !== "") {
      writer.uint32(26).string(message.apiEndpoint);
    }
    if (message.connectionType !== "") {
      writer.uint32(34).string(message.connectionType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chainId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.apiEndpoint = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.connectionType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageInfo {
    return {
      channelId: isSet(object.channelId)
        ? globalThis.String(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.String(object.channel_id)
        : "",
      chainId: isSet(object.chainId)
        ? globalThis.String(object.chainId)
        : isSet(object.chain_id)
        ? globalThis.String(object.chain_id)
        : "",
      apiEndpoint: isSet(object.apiEndpoint)
        ? globalThis.String(object.apiEndpoint)
        : isSet(object.api_endpoint)
        ? globalThis.String(object.api_endpoint)
        : "",
      connectionType: isSet(object.connectionType)
        ? globalThis.String(object.connectionType)
        : isSet(object.connection_type)
        ? globalThis.String(object.connection_type)
        : "",
    };
  },

  toJSON(message: StorageInfo): unknown {
    const obj: any = {};
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    if (message.chainId !== "") {
      obj.chainId = message.chainId;
    }
    if (message.apiEndpoint !== "") {
      obj.apiEndpoint = message.apiEndpoint;
    }
    if (message.connectionType !== "") {
      obj.connectionType = message.connectionType;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageInfo>, I>>(base?: I): StorageInfo {
    return StorageInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageInfo>, I>>(object: I): StorageInfo {
    const message = createBaseStorageInfo();
    message.channelId = object.channelId ?? "";
    message.chainId = object.chainId ?? "";
    message.apiEndpoint = object.apiEndpoint ?? "";
    message.connectionType = object.connectionType ?? "";
    return message;
  },
};

function createBaseMerkleStep(): MerkleStep {
  return { siblingHex: "", siblingIsLeft: false };
}

export const MerkleStep: MessageFns<MerkleStep> = {
  encode(message: MerkleStep, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.siblingHex !== "") {
      writer.uint32(10).string(message.siblingHex);
    }
    if (message.siblingIsLeft !== false) {
      writer.uint32(16).bool(message.siblingIsLeft);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MerkleStep {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMerkleStep();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.siblingHex = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.siblingIsLeft = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MerkleStep {
    return {
      siblingHex: isSet(object.siblingHex)
        ? globalThis.String(object.siblingHex)
        : isSet(object.sibling_hex)
        ? globalThis.String(object.sibling_hex)
        : "",
      siblingIsLeft: isSet(object.siblingIsLeft)
        ? globalThis.Boolean(object.siblingIsLeft)
        : isSet(object.sibling_is_left)
        ? globalThis.Boolean(object.sibling_is_left)
        : false,
    };
  },

  toJSON(message: MerkleStep): unknown {
    const obj: any = {};
    if (message.siblingHex !== "") {
      obj.siblingHex = message.siblingHex;
    }
    if (message.siblingIsLeft !== false) {
      obj.siblingIsLeft = message.siblingIsLeft;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MerkleStep>, I>>(base?: I): MerkleStep {
    return MerkleStep.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MerkleStep>, I>>(object: I): MerkleStep {
    const message = createBaseMerkleStep();
    message.siblingHex = object.siblingHex ?? "";
    message.siblingIsLeft = object.siblingIsLeft ?? false;
    return message;
  },
};

function createBaseMerkleProof(): MerkleProof {
  return { steps: [] };
}

export const MerkleProof: MessageFns<MerkleProof> = {
  encode(message: MerkleProof, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.steps) {
      MerkleStep.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MerkleProof {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMerkleProof();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.steps.push(MerkleStep.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MerkleProof {
    return {
      steps: globalThis.Array.isArray(object?.steps) ? object.steps.map((e: any) => MerkleStep.fromJSON(e)) : [],
    };
  },

  toJSON(message: MerkleProof): unknown {
    const obj: any = {};
    if (message.steps?.length) {
      obj.steps = message.steps.map((e) => MerkleStep.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MerkleProof>, I>>(base?: I): MerkleProof {
    return MerkleProof.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MerkleProof>, I>>(object: I): MerkleProof {
    const message = createBaseMerkleProof();
    message.steps = object.steps?.map((e) => MerkleStep.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSession(): Session {
  return {
    sessionId: "",
    owner: "",
    executor: "",
    rootProofHex: "",
    fragmentSize: Long.UZERO,
    deadlineUnix: Long.ZERO,
    state: 0,
    closeReason: "",
    distributedCount: Long.UZERO,
    ackSuccessCount: Long.UZERO,
    ackErrorCount: Long.UZERO,
  };
}

export const Session: MessageFns<Session> = {
  encode(message: Session, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.owner !== "") {
      writer.uint32(18).string(message.owner);
    }
    if (message.executor !== "") {
      writer.uint32(26).string(message.executor);
    }
    if (message.rootProofHex !== "") {
      writer.uint32(34).string(message.rootProofHex);
    }
    if (!message.fragmentSize.equals(Long.UZERO)) {
      writer.uint32(40).uint64(message.fragmentSize.toString());
    }
    if (!message.deadlineUnix.equals(Long.ZERO)) {
      writer.uint32(48).int64(message.deadlineUnix.toString());
    }
    if (message.state !== 0) {
      writer.uint32(56).int32(message.state);
    }
    if (message.closeReason !== "") {
      writer.uint32(66).string(message.closeReason);
    }
    if (!message.distributedCount.equals(Long.UZERO)) {
      writer.uint32(72).uint64(message.distributedCount.toString());
    }
    if (!message.ackSuccessCount.equals(Long.UZERO)) {
      writer.uint32(80).uint64(message.ackSuccessCount.toString());
    }
    if (!message.ackErrorCount.equals(Long.UZERO)) {
      writer.uint32(88).uint64(message.ackErrorCount.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Session {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.executor = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.rootProofHex = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.fragmentSize = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.deadlineUnix = Long.fromString(reader.int64().toString());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.closeReason = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.distributedCount = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.ackSuccessCount = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.ackErrorCount = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Session {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      executor: isSet(object.executor) ? globalThis.String(object.executor) : "",
      rootProofHex: isSet(object.rootProofHex)
        ? globalThis.String(object.rootProofHex)
        : isSet(object.root_proof_hex)
        ? globalThis.String(object.root_proof_hex)
        : "",
      fragmentSize: isSet(object.fragmentSize)
        ? Long.fromValue(object.fragmentSize)
        : isSet(object.fragment_size)
        ? Long.fromValue(object.fragment_size)
        : Long.UZERO,
      deadlineUnix: isSet(object.deadlineUnix)
        ? Long.fromValue(object.deadlineUnix)
        : isSet(object.deadline_unix)
        ? Long.fromValue(object.deadline_unix)
        : Long.ZERO,
      state: isSet(object.state) ? sessionStateFromJSON(object.state) : 0,
      closeReason: isSet(object.closeReason)
        ? globalThis.String(object.closeReason)
        : isSet(object.close_reason)
        ? globalThis.String(object.close_reason)
        : "",
      distributedCount: isSet(object.distributedCount)
        ? Long.fromValue(object.distributedCount)
        : isSet(object.distributed_count)
        ? Long.fromValue(object.distributed_count)
        : Long.UZERO,
      ackSuccessCount: isSet(object.ackSuccessCount)
        ? Long.fromValue(object.ackSuccessCount)
        : isSet(object.ack_success_count)
        ? Long.fromValue(object.ack_success_count)
        : Long.UZERO,
      ackErrorCount: isSet(object.ackErrorCount)
        ? Long.fromValue(object.ackErrorCount)
        : isSet(object.ack_error_count)
        ? Long.fromValue(object.ack_error_count)
        : Long.UZERO,
    };
  },

  toJSON(message: Session): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.executor !== "") {
      obj.executor = message.executor;
    }
    if (message.rootProofHex !== "") {
      obj.rootProofHex = message.rootProofHex;
    }
    if (!message.fragmentSize.equals(Long.UZERO)) {
      obj.fragmentSize = (message.fragmentSize || Long.UZERO).toString();
    }
    if (!message.deadlineUnix.equals(Long.ZERO)) {
      obj.deadlineUnix = (message.deadlineUnix || Long.ZERO).toString();
    }
    if (message.state !== 0) {
      obj.state = sessionStateToJSON(message.state);
    }
    if (message.closeReason !== "") {
      obj.closeReason = message.closeReason;
    }
    if (!message.distributedCount.equals(Long.UZERO)) {
      obj.distributedCount = (message.distributedCount || Long.UZERO).toString();
    }
    if (!message.ackSuccessCount.equals(Long.UZERO)) {
      obj.ackSuccessCount = (message.ackSuccessCount || Long.UZERO).toString();
    }
    if (!message.ackErrorCount.equals(Long.UZERO)) {
      obj.ackErrorCount = (message.ackErrorCount || Long.UZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Session>, I>>(base?: I): Session {
    return Session.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Session>, I>>(object: I): Session {
    const message = createBaseSession();
    message.sessionId = object.sessionId ?? "";
    message.owner = object.owner ?? "";
    message.executor = object.executor ?? "";
    message.rootProofHex = object.rootProofHex ?? "";
    message.fragmentSize = (object.fragmentSize !== undefined && object.fragmentSize !== null)
      ? Long.fromValue(object.fragmentSize)
      : Long.UZERO;
    message.deadlineUnix = (object.deadlineUnix !== undefined && object.deadlineUnix !== null)
      ? Long.fromValue(object.deadlineUnix)
      : Long.ZERO;
    message.state = object.state ?? 0;
    message.closeReason = object.closeReason ?? "";
    message.distributedCount = (object.distributedCount !== undefined && object.distributedCount !== null)
      ? Long.fromValue(object.distributedCount)
      : Long.UZERO;
    message.ackSuccessCount = (object.ackSuccessCount !== undefined && object.ackSuccessCount !== null)
      ? Long.fromValue(object.ackSuccessCount)
      : Long.UZERO;
    message.ackErrorCount = (object.ackErrorCount !== undefined && object.ackErrorCount !== null)
      ? Long.fromValue(object.ackErrorCount)
      : Long.UZERO;
    return message;
  },
};

function createBaseDistributeItem(): DistributeItem {
  return {
    path: "",
    index: Long.UZERO,
    fragmentBytes: new Uint8Array(0),
    fragmentProof: undefined,
    fileSize: Long.UZERO,
    fileProof: undefined,
    targetFdscChannel: "",
  };
}

export const DistributeItem: MessageFns<DistributeItem> = {
  encode(message: DistributeItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (!message.index.equals(Long.UZERO)) {
      writer.uint32(16).uint64(message.index.toString());
    }
    if (message.fragmentBytes.length !== 0) {
      writer.uint32(26).bytes(message.fragmentBytes);
    }
    if (message.fragmentProof !== undefined) {
      MerkleProof.encode(message.fragmentProof, writer.uint32(34).fork()).join();
    }
    if (!message.fileSize.equals(Long.UZERO)) {
      writer.uint32(40).uint64(message.fileSize.toString());
    }
    if (message.fileProof !== undefined) {
      MerkleProof.encode(message.fileProof, writer.uint32(50).fork()).join();
    }
    if (message.targetFdscChannel !== "") {
      writer.uint32(58).string(message.targetFdscChannel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DistributeItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDistributeItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.index = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fragmentBytes = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.fragmentProof = MerkleProof.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.fileSize = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.fileProof = MerkleProof.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.targetFdscChannel = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DistributeItem {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      index: isSet(object.index) ? Long.fromValue(object.index) : Long.UZERO,
      fragmentBytes: isSet(object.fragmentBytes)
        ? bytesFromBase64(object.fragmentBytes)
        : isSet(object.fragment_bytes)
        ? bytesFromBase64(object.fragment_bytes)
        : new Uint8Array(0),
      fragmentProof: isSet(object.fragmentProof)
        ? MerkleProof.fromJSON(object.fragmentProof)
        : isSet(object.fragment_proof)
        ? MerkleProof.fromJSON(object.fragment_proof)
        : undefined,
      fileSize: isSet(object.fileSize)
        ? Long.fromValue(object.fileSize)
        : isSet(object.file_size)
        ? Long.fromValue(object.file_size)
        : Long.UZERO,
      fileProof: isSet(object.fileProof)
        ? MerkleProof.fromJSON(object.fileProof)
        : isSet(object.file_proof)
        ? MerkleProof.fromJSON(object.file_proof)
        : undefined,
      targetFdscChannel: isSet(object.targetFdscChannel)
        ? globalThis.String(object.targetFdscChannel)
        : isSet(object.target_fdsc_channel)
        ? globalThis.String(object.target_fdsc_channel)
        : "",
    };
  },

  toJSON(message: DistributeItem): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (!message.index.equals(Long.UZERO)) {
      obj.index = (message.index || Long.UZERO).toString();
    }
    if (message.fragmentBytes.length !== 0) {
      obj.fragmentBytes = base64FromBytes(message.fragmentBytes);
    }
    if (message.fragmentProof !== undefined) {
      obj.fragmentProof = MerkleProof.toJSON(message.fragmentProof);
    }
    if (!message.fileSize.equals(Long.UZERO)) {
      obj.fileSize = (message.fileSize || Long.UZERO).toString();
    }
    if (message.fileProof !== undefined) {
      obj.fileProof = MerkleProof.toJSON(message.fileProof);
    }
    if (message.targetFdscChannel !== "") {
      obj.targetFdscChannel = message.targetFdscChannel;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DistributeItem>, I>>(base?: I): DistributeItem {
    return DistributeItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DistributeItem>, I>>(object: I): DistributeItem {
    const message = createBaseDistributeItem();
    message.path = object.path ?? "";
    message.index = (object.index !== undefined && object.index !== null) ? Long.fromValue(object.index) : Long.UZERO;
    message.fragmentBytes = object.fragmentBytes ?? new Uint8Array(0);
    message.fragmentProof = (object.fragmentProof !== undefined && object.fragmentProof !== null)
      ? MerkleProof.fromPartial(object.fragmentProof)
      : undefined;
    message.fileSize = (object.fileSize !== undefined && object.fileSize !== null)
      ? Long.fromValue(object.fileSize)
      : Long.UZERO;
    message.fileProof = (object.fileProof !== undefined && object.fileProof !== null)
      ? MerkleProof.fromPartial(object.fileProof)
      : undefined;
    message.targetFdscChannel = object.targetFdscChannel ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}

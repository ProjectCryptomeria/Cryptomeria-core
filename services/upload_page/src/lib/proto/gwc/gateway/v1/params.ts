// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: gwc/gateway/v1/params.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "gwc.gateway.v1";

/** Params defines the parameters for the module. */
export interface Params {
  /**
   * max_fragment_bytes is the maximum allowed size of a single fragment.
   * This is enforced for:
   * - MsgInitSession.fragment_size
   * - MsgDistributeBatch.items[].fragment_bytes length
   */
  maxFragmentBytes: Long;
  /**
   * max_fragments_per_session limits the total number of fragments distributable in a single session.
   * This is enforced in MsgDistributeBatch using session.distributed_count (+ incoming batch size).
   */
  maxFragmentsPerSession: Long;
  /** default_deadline_seconds is used when MsgInitSession.deadline_unix == 0. */
  defaultDeadlineSeconds: Long;
  /**
   * local_admin is the fixed executor address (aka "local-admin") for CSU.
   * CSU protocol requires executor fixed to local-admin.
   * If empty, CSU executor enforcement will fail (InitSession/Distribute/Finalize/Abort will be rejected).
   */
  localAdmin: string;
}

function createBaseParams(): Params {
  return {
    maxFragmentBytes: Long.UZERO,
    maxFragmentsPerSession: Long.UZERO,
    defaultDeadlineSeconds: Long.ZERO,
    localAdmin: "",
  };
}

export const Params: MessageFns<Params> = {
  encode(message: Params, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.maxFragmentBytes.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.maxFragmentBytes.toString());
    }
    if (!message.maxFragmentsPerSession.equals(Long.UZERO)) {
      writer.uint32(16).uint64(message.maxFragmentsPerSession.toString());
    }
    if (!message.defaultDeadlineSeconds.equals(Long.ZERO)) {
      writer.uint32(24).int64(message.defaultDeadlineSeconds.toString());
    }
    if (message.localAdmin !== "") {
      writer.uint32(34).string(message.localAdmin);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Params {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxFragmentBytes = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxFragmentsPerSession = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.defaultDeadlineSeconds = Long.fromString(reader.int64().toString());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.localAdmin = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      maxFragmentBytes: isSet(object.maxFragmentBytes)
        ? Long.fromValue(object.maxFragmentBytes)
        : isSet(object.max_fragment_bytes)
        ? Long.fromValue(object.max_fragment_bytes)
        : Long.UZERO,
      maxFragmentsPerSession: isSet(object.maxFragmentsPerSession)
        ? Long.fromValue(object.maxFragmentsPerSession)
        : isSet(object.max_fragments_per_session)
        ? Long.fromValue(object.max_fragments_per_session)
        : Long.UZERO,
      defaultDeadlineSeconds: isSet(object.defaultDeadlineSeconds)
        ? Long.fromValue(object.defaultDeadlineSeconds)
        : isSet(object.default_deadline_seconds)
        ? Long.fromValue(object.default_deadline_seconds)
        : Long.ZERO,
      localAdmin: isSet(object.localAdmin)
        ? globalThis.String(object.localAdmin)
        : isSet(object.local_admin)
        ? globalThis.String(object.local_admin)
        : "",
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (!message.maxFragmentBytes.equals(Long.UZERO)) {
      obj.maxFragmentBytes = (message.maxFragmentBytes || Long.UZERO).toString();
    }
    if (!message.maxFragmentsPerSession.equals(Long.UZERO)) {
      obj.maxFragmentsPerSession = (message.maxFragmentsPerSession || Long.UZERO).toString();
    }
    if (!message.defaultDeadlineSeconds.equals(Long.ZERO)) {
      obj.defaultDeadlineSeconds = (message.defaultDeadlineSeconds || Long.ZERO).toString();
    }
    if (message.localAdmin !== "") {
      obj.localAdmin = message.localAdmin;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Params>, I>>(base?: I): Params {
    return Params.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Params>, I>>(object: I): Params {
    const message = createBaseParams();
    message.maxFragmentBytes = (object.maxFragmentBytes !== undefined && object.maxFragmentBytes !== null)
      ? Long.fromValue(object.maxFragmentBytes)
      : Long.UZERO;
    message.maxFragmentsPerSession =
      (object.maxFragmentsPerSession !== undefined && object.maxFragmentsPerSession !== null)
        ? Long.fromValue(object.maxFragmentsPerSession)
        : Long.UZERO;
    message.defaultDeadlineSeconds =
      (object.defaultDeadlineSeconds !== undefined && object.defaultDeadlineSeconds !== null)
        ? Long.fromValue(object.defaultDeadlineSeconds)
        : Long.ZERO;
    message.localAdmin = object.localAdmin ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}

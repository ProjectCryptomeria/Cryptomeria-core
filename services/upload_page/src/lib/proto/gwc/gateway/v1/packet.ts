// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: gwc/gateway/v1/packet.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "gwc.gateway.v1";

/** GatewayPacketData defines the packet data for the gateway module */
export interface GatewayPacketData {
  noDataPacket?: NoDataPacket | undefined;
  fragmentPacket?: FragmentPacket | undefined;
  manifestPacket?: ManifestPacket | undefined;
}

export interface NoDataPacket {
}

/** FragmentPacket defines a single fragment of file data */
export interface FragmentPacket {
  sessionId: string;
  /** RootProof for verification reference */
  rootProof: string;
  /** identifies the fragment position */
  path: string;
  index: Long;
  data: Uint8Array;
}

/** PacketFragmentMapping defines the location of a fragment */
export interface PacketFragmentMapping {
  fdscId: string;
  fragmentId: string;
}

/** FileMetadata defines metadata for a single file within the project */
export interface FileMetadata {
  mimeType: string;
  fileSize: Long;
  fragments: PacketFragmentMapping[];
  /** file_root is the Merkle root of fragments in this file */
  fileRoot: string;
}

/** ManifestPacket defines the structure of the website/project */
export interface ManifestPacket {
  projectName: string;
  version: string;
  /**
   * 修正点: mapはGogoProtoとCosmos SDKのPulsarリフレクションで互換性がないため、
   * 独自のEntry構造体を使用したrepeatedに変更します。
   */
  files: ManifestFileEntry[];
  /** CSU verification fields */
  rootProof: string;
  fragmentSize: Long;
  /** identity fields */
  owner: string;
  sessionId: string;
}

/** ManifestFileEntry は map<string, FileMetadata> の代替となるエントリ構造体です */
export interface ManifestFileEntry {
  path: string;
  metadata?: FileMetadata | undefined;
}

function createBaseGatewayPacketData(): GatewayPacketData {
  return { noDataPacket: undefined, fragmentPacket: undefined, manifestPacket: undefined };
}

export const GatewayPacketData: MessageFns<GatewayPacketData> = {
  encode(message: GatewayPacketData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.noDataPacket !== undefined) {
      NoDataPacket.encode(message.noDataPacket, writer.uint32(10).fork()).join();
    }
    if (message.fragmentPacket !== undefined) {
      FragmentPacket.encode(message.fragmentPacket, writer.uint32(18).fork()).join();
    }
    if (message.manifestPacket !== undefined) {
      ManifestPacket.encode(message.manifestPacket, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GatewayPacketData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGatewayPacketData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.noDataPacket = NoDataPacket.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fragmentPacket = FragmentPacket.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.manifestPacket = ManifestPacket.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GatewayPacketData {
    return {
      noDataPacket: isSet(object.noDataPacket)
        ? NoDataPacket.fromJSON(object.noDataPacket)
        : isSet(object.no_data_packet)
        ? NoDataPacket.fromJSON(object.no_data_packet)
        : undefined,
      fragmentPacket: isSet(object.fragmentPacket)
        ? FragmentPacket.fromJSON(object.fragmentPacket)
        : isSet(object.fragment_packet)
        ? FragmentPacket.fromJSON(object.fragment_packet)
        : undefined,
      manifestPacket: isSet(object.manifestPacket)
        ? ManifestPacket.fromJSON(object.manifestPacket)
        : isSet(object.manifest_packet)
        ? ManifestPacket.fromJSON(object.manifest_packet)
        : undefined,
    };
  },

  toJSON(message: GatewayPacketData): unknown {
    const obj: any = {};
    if (message.noDataPacket !== undefined) {
      obj.noDataPacket = NoDataPacket.toJSON(message.noDataPacket);
    }
    if (message.fragmentPacket !== undefined) {
      obj.fragmentPacket = FragmentPacket.toJSON(message.fragmentPacket);
    }
    if (message.manifestPacket !== undefined) {
      obj.manifestPacket = ManifestPacket.toJSON(message.manifestPacket);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GatewayPacketData>, I>>(base?: I): GatewayPacketData {
    return GatewayPacketData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GatewayPacketData>, I>>(object: I): GatewayPacketData {
    const message = createBaseGatewayPacketData();
    message.noDataPacket = (object.noDataPacket !== undefined && object.noDataPacket !== null)
      ? NoDataPacket.fromPartial(object.noDataPacket)
      : undefined;
    message.fragmentPacket = (object.fragmentPacket !== undefined && object.fragmentPacket !== null)
      ? FragmentPacket.fromPartial(object.fragmentPacket)
      : undefined;
    message.manifestPacket = (object.manifestPacket !== undefined && object.manifestPacket !== null)
      ? ManifestPacket.fromPartial(object.manifestPacket)
      : undefined;
    return message;
  },
};

function createBaseNoDataPacket(): NoDataPacket {
  return {};
}

export const NoDataPacket: MessageFns<NoDataPacket> = {
  encode(_: NoDataPacket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NoDataPacket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNoDataPacket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): NoDataPacket {
    return {};
  },

  toJSON(_: NoDataPacket): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<NoDataPacket>, I>>(base?: I): NoDataPacket {
    return NoDataPacket.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NoDataPacket>, I>>(_: I): NoDataPacket {
    const message = createBaseNoDataPacket();
    return message;
  },
};

function createBaseFragmentPacket(): FragmentPacket {
  return { sessionId: "", rootProof: "", path: "", index: Long.UZERO, data: new Uint8Array(0) };
}

export const FragmentPacket: MessageFns<FragmentPacket> = {
  encode(message: FragmentPacket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.rootProof !== "") {
      writer.uint32(18).string(message.rootProof);
    }
    if (message.path !== "") {
      writer.uint32(26).string(message.path);
    }
    if (!message.index.equals(Long.UZERO)) {
      writer.uint32(32).uint64(message.index.toString());
    }
    if (message.data.length !== 0) {
      writer.uint32(42).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FragmentPacket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFragmentPacket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rootProof = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.index = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FragmentPacket {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      rootProof: isSet(object.rootProof)
        ? globalThis.String(object.rootProof)
        : isSet(object.root_proof)
        ? globalThis.String(object.root_proof)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      index: isSet(object.index) ? Long.fromValue(object.index) : Long.UZERO,
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
    };
  },

  toJSON(message: FragmentPacket): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.rootProof !== "") {
      obj.rootProof = message.rootProof;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (!message.index.equals(Long.UZERO)) {
      obj.index = (message.index || Long.UZERO).toString();
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FragmentPacket>, I>>(base?: I): FragmentPacket {
    return FragmentPacket.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FragmentPacket>, I>>(object: I): FragmentPacket {
    const message = createBaseFragmentPacket();
    message.sessionId = object.sessionId ?? "";
    message.rootProof = object.rootProof ?? "";
    message.path = object.path ?? "";
    message.index = (object.index !== undefined && object.index !== null) ? Long.fromValue(object.index) : Long.UZERO;
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBasePacketFragmentMapping(): PacketFragmentMapping {
  return { fdscId: "", fragmentId: "" };
}

export const PacketFragmentMapping: MessageFns<PacketFragmentMapping> = {
  encode(message: PacketFragmentMapping, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fdscId !== "") {
      writer.uint32(10).string(message.fdscId);
    }
    if (message.fragmentId !== "") {
      writer.uint32(18).string(message.fragmentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PacketFragmentMapping {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePacketFragmentMapping();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fdscId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fragmentId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PacketFragmentMapping {
    return {
      fdscId: isSet(object.fdscId)
        ? globalThis.String(object.fdscId)
        : isSet(object.fdsc_id)
        ? globalThis.String(object.fdsc_id)
        : "",
      fragmentId: isSet(object.fragmentId)
        ? globalThis.String(object.fragmentId)
        : isSet(object.fragment_id)
        ? globalThis.String(object.fragment_id)
        : "",
    };
  },

  toJSON(message: PacketFragmentMapping): unknown {
    const obj: any = {};
    if (message.fdscId !== "") {
      obj.fdscId = message.fdscId;
    }
    if (message.fragmentId !== "") {
      obj.fragmentId = message.fragmentId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PacketFragmentMapping>, I>>(base?: I): PacketFragmentMapping {
    return PacketFragmentMapping.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PacketFragmentMapping>, I>>(object: I): PacketFragmentMapping {
    const message = createBasePacketFragmentMapping();
    message.fdscId = object.fdscId ?? "";
    message.fragmentId = object.fragmentId ?? "";
    return message;
  },
};

function createBaseFileMetadata(): FileMetadata {
  return { mimeType: "", fileSize: Long.UZERO, fragments: [], fileRoot: "" };
}

export const FileMetadata: MessageFns<FileMetadata> = {
  encode(message: FileMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mimeType !== "") {
      writer.uint32(10).string(message.mimeType);
    }
    if (!message.fileSize.equals(Long.UZERO)) {
      writer.uint32(16).uint64(message.fileSize.toString());
    }
    for (const v of message.fragments) {
      PacketFragmentMapping.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.fileRoot !== "") {
      writer.uint32(34).string(message.fileRoot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.fileSize = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fragments.push(PacketFragmentMapping.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.fileRoot = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileMetadata {
    return {
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
      fileSize: isSet(object.fileSize)
        ? Long.fromValue(object.fileSize)
        : isSet(object.file_size)
        ? Long.fromValue(object.file_size)
        : Long.UZERO,
      fragments: globalThis.Array.isArray(object?.fragments)
        ? object.fragments.map((e: any) => PacketFragmentMapping.fromJSON(e))
        : [],
      fileRoot: isSet(object.fileRoot)
        ? globalThis.String(object.fileRoot)
        : isSet(object.file_root)
        ? globalThis.String(object.file_root)
        : "",
    };
  },

  toJSON(message: FileMetadata): unknown {
    const obj: any = {};
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (!message.fileSize.equals(Long.UZERO)) {
      obj.fileSize = (message.fileSize || Long.UZERO).toString();
    }
    if (message.fragments?.length) {
      obj.fragments = message.fragments.map((e) => PacketFragmentMapping.toJSON(e));
    }
    if (message.fileRoot !== "") {
      obj.fileRoot = message.fileRoot;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileMetadata>, I>>(base?: I): FileMetadata {
    return FileMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileMetadata>, I>>(object: I): FileMetadata {
    const message = createBaseFileMetadata();
    message.mimeType = object.mimeType ?? "";
    message.fileSize = (object.fileSize !== undefined && object.fileSize !== null)
      ? Long.fromValue(object.fileSize)
      : Long.UZERO;
    message.fragments = object.fragments?.map((e) => PacketFragmentMapping.fromPartial(e)) || [];
    message.fileRoot = object.fileRoot ?? "";
    return message;
  },
};

function createBaseManifestPacket(): ManifestPacket {
  return { projectName: "", version: "", files: [], rootProof: "", fragmentSize: Long.UZERO, owner: "", sessionId: "" };
}

export const ManifestPacket: MessageFns<ManifestPacket> = {
  encode(message: ManifestPacket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.projectName !== "") {
      writer.uint32(10).string(message.projectName);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    for (const v of message.files) {
      ManifestFileEntry.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.rootProof !== "") {
      writer.uint32(34).string(message.rootProof);
    }
    if (!message.fragmentSize.equals(Long.UZERO)) {
      writer.uint32(40).uint64(message.fragmentSize.toString());
    }
    if (message.owner !== "") {
      writer.uint32(50).string(message.owner);
    }
    if (message.sessionId !== "") {
      writer.uint32(58).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ManifestPacket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseManifestPacket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.projectName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.files.push(ManifestFileEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.rootProof = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.fragmentSize = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ManifestPacket {
    return {
      projectName: isSet(object.projectName)
        ? globalThis.String(object.projectName)
        : isSet(object.project_name)
        ? globalThis.String(object.project_name)
        : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      files: globalThis.Array.isArray(object?.files) ? object.files.map((e: any) => ManifestFileEntry.fromJSON(e)) : [],
      rootProof: isSet(object.rootProof)
        ? globalThis.String(object.rootProof)
        : isSet(object.root_proof)
        ? globalThis.String(object.root_proof)
        : "",
      fragmentSize: isSet(object.fragmentSize)
        ? Long.fromValue(object.fragmentSize)
        : isSet(object.fragment_size)
        ? Long.fromValue(object.fragment_size)
        : Long.UZERO,
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: ManifestPacket): unknown {
    const obj: any = {};
    if (message.projectName !== "") {
      obj.projectName = message.projectName;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.files?.length) {
      obj.files = message.files.map((e) => ManifestFileEntry.toJSON(e));
    }
    if (message.rootProof !== "") {
      obj.rootProof = message.rootProof;
    }
    if (!message.fragmentSize.equals(Long.UZERO)) {
      obj.fragmentSize = (message.fragmentSize || Long.UZERO).toString();
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ManifestPacket>, I>>(base?: I): ManifestPacket {
    return ManifestPacket.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ManifestPacket>, I>>(object: I): ManifestPacket {
    const message = createBaseManifestPacket();
    message.projectName = object.projectName ?? "";
    message.version = object.version ?? "";
    message.files = object.files?.map((e) => ManifestFileEntry.fromPartial(e)) || [];
    message.rootProof = object.rootProof ?? "";
    message.fragmentSize = (object.fragmentSize !== undefined && object.fragmentSize !== null)
      ? Long.fromValue(object.fragmentSize)
      : Long.UZERO;
    message.owner = object.owner ?? "";
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseManifestFileEntry(): ManifestFileEntry {
  return { path: "", metadata: undefined };
}

export const ManifestFileEntry: MessageFns<ManifestFileEntry> = {
  encode(message: ManifestFileEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.metadata !== undefined) {
      FileMetadata.encode(message.metadata, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ManifestFileEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseManifestFileEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.metadata = FileMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ManifestFileEntry {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      metadata: isSet(object.metadata) ? FileMetadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: ManifestFileEntry): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.metadata !== undefined) {
      obj.metadata = FileMetadata.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ManifestFileEntry>, I>>(base?: I): ManifestFileEntry {
    return ManifestFileEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ManifestFileEntry>, I>>(object: I): ManifestFileEntry {
    const message = createBaseManifestFileEntry();
    message.path = object.path ?? "";
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? FileMetadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}

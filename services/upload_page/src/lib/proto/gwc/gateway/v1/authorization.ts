// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: gwc/gateway/v1/authorization.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "gwc.gateway.v1";

/**
 * SessionBoundAuthorization is an authz Authorization which binds permission to a specific session_id.
 * It is intended to be granted by the session owner (granter) to local-admin (grantee).
 *
 * - session_id: the CSU session id this grant is bound to.
 * - msg_type_url: the sdk.Msg type URL allowed by this authorization (e.g. "/gwc.gateway.v1.MsgDistributeBatch").
 *
 * NOTE:
 * Authz grants are keyed by MsgTypeURL, so this authorization is typically granted one-per-message-type,
 * but all share the same session_id.
 */
export interface SessionBoundAuthorization {
  sessionId: string;
  msgTypeUrl: string;
}

function createBaseSessionBoundAuthorization(): SessionBoundAuthorization {
  return { sessionId: "", msgTypeUrl: "" };
}

export const SessionBoundAuthorization: MessageFns<SessionBoundAuthorization> = {
  encode(message: SessionBoundAuthorization, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.msgTypeUrl !== "") {
      writer.uint32(18).string(message.msgTypeUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionBoundAuthorization {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionBoundAuthorization();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.msgTypeUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionBoundAuthorization {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      msgTypeUrl: isSet(object.msgTypeUrl)
        ? globalThis.String(object.msgTypeUrl)
        : isSet(object.msg_type_url)
        ? globalThis.String(object.msg_type_url)
        : "",
    };
  },

  toJSON(message: SessionBoundAuthorization): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.msgTypeUrl !== "") {
      obj.msgTypeUrl = message.msgTypeUrl;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionBoundAuthorization>, I>>(base?: I): SessionBoundAuthorization {
    return SessionBoundAuthorization.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionBoundAuthorization>, I>>(object: I): SessionBoundAuthorization {
    const message = createBaseSessionBoundAuthorization();
    message.sessionId = object.sessionId ?? "";
    message.msgTypeUrl = object.msgTypeUrl ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
